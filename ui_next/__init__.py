"""Next-generation GUI package for EIDAT.

Contains a Qt-based UI (`qt_main.py`) and a thin backend adapter (`backend.py`)
that wraps the existing scripts and batch runner. Meant to coexist with the
current Tkinter `gui.py` while we iterate on design.
"""

