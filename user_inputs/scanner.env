# Scanner configuration (session-scoped). Lines of the form KEY=VALUE.
# Blank lines and lines starting with # or ; are ignored.

# --- OCRmyPDF controls ---
#Enable OCRmyPDF pre-processing: primary (pre-OCR entire doc) or fallback (truthy).
USE_OCRMYPDF=off

# Force OCR of pages even if they contain some embedded text (helps with image-only pages)
#OCRMYPDF_FORCE=1
FORCE_OCR=1

# Language and optimizSation level (0..3). Higher levels may require external tools (pngquant).
OCRMYPDF_LANG=eng
OCRMYPDF_OPTIMIZE=1

# Keep temporary OCR’d PDFs for debugging (1=true).
#OCRMYPDF_KEEP=0

# --- Direct Tesseract OCR controls (when used) ---
OCR_RENDERER=easyocr
OCR_DPI=800
#TESSERACT_ARGS=--psm 6

# Optional paths for tools if not on PATH
#TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe
PYTHONPATH=C:\Users\zachs\Documents\DevProjects\PDF_Scanner\Lib\site-packages;%PYTHONPATH%

# --- EasyOCR spatial XY controls ---
USE_EASYOCR_XY=1
EASYOCR_LANGS=en
XY_FUZZ=0.75
ROW_BAND=0.6
COL_TOL=0.6
